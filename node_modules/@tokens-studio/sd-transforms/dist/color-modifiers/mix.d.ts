import Color from 'colorjs.io';
import { ColorSpaceTypes } from '@tokens-studio/types';
export declare function mix(color: Color, colorSpace: ColorSpaceTypes, amount: number, mixColor: Color): Color;
